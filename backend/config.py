class Config:
    # Allowed origins for CORS
    ORIGINS = ["http://localhost:5173"]
    
    # Port number where the Flask app will run
    PORT = 8080
    
    # Enable or disable debug mode (set to True for development, False for production)
    DEBUG = True
    